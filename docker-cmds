Elastic-search :- docker run --rm  --name kibana   -p 5601:5601    --link elasticsearch:elasticsearch  -e "ELASTICSEARCH_URL=http://elasticsearch:9200" docker.elastic.co/kibana/kibana:6.6.2

Kibana :- docker run --rm  --name kibana   -p 5601:5601    --link elasticsearch:elasticsearch  -e "ELASTICSEARCH_URL=http://elasticsearch:9200" docker.elastic.co/kibana/kibana:6.6.2

version of both used :- 6.6.2
